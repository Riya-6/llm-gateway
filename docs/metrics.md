# Benchmark results log

Log every load test / chaos test / benchmark run here, with real numbers —
not "it worked." If you report running a test without pasting results here,
expect to get asked for them before it counts as done.

| Date | Feature tested | Tool used | Config | Key numbers | Notes |
|---|---|---|---|---|---|
| 2026-08-24 | Chaos test (generation pipeline) | scripts/chaos_test.py | requests=45, fail_after=5, fail_for=5, mode=error, GENERATION_MOCK_ONLY=true (single provider — no second provider to fall back to, see notes) | success_rate=0% during failure window, first_error_response=1 req into failure window (proper 502/503, not a hang), recovery=21 req after failure window ended (~30s recovery_timeout_seconds observed live) | raw results: backend\scripts\results\chaos_test_20260824T105049Z.csv — see docs/decisions.md 2026-08-24 entries for the debugging history (localhost latency, mock-only chaos target) behind getting to this clean run |
| 2026-08-24 | Chaos test — real fallback handoff (generation pipeline) | scripts/chaos_test.py | requests=20, fail_after=5, fail_for=8, mode=error, two-provider config: GENERATION_MOCK_ONLY=true + MOCK_FALLBACK_PROVIDER_URL set (primary mock on :9100 forced into error mode, fallback mock on :9101 stayed normal throughout) | success_rate=100% during failure window, first_error_response=None (fallback absorbed every failure, client never saw an error), recovery=1 req (post-window responses kept coming from fallback since primary's breaker hadn't reached its 30s recovery_timeout_seconds yet — expected), served_by_during_failure=['mock-fallback'] (verified per-request from response body, not inferred from status codes) | raw results: backend\scripts\results\chaos_test_20260824T110554Z.csv |
| 2026-08-24 | Chaos test — real fallback handoff, real provider (generation pipeline) | scripts/chaos_test.py | requests=10, fail_after=3, fail_for=4, mode=error, model=gpt-5-nano, timeout=30s, config: GENERATION_MOCK_ONLY=true + GENERATION_REAL_FALLBACK_PROVIDER=openai (mock forced into error mode as primary, real OpenAI as the sole fallback route) | success_rate=100% during failure window, first_error_response=None (fallback absorbed every failure, client never saw an error), recovery confirmed at ~30.1-30.6s real elapsed wall-clock time (recovery_timeout_seconds=30s) — computed from CSV timestamps: breaker opened during request 6 (its 3rd consecutive mock-primary failure, ~17:04:18.955-19.455) and the first successful post-recovery request (8) started at 17:04:49.544745, both bounds landing at/above the 30s threshold, so this is a genuine confirmation and not a coincidence of request count, served_by_during_failure=['openai'] (verified per-request from response body — real OpenAI latency observed 12-27s per call, notably higher than gpt-4o-mini in earlier sanity checks) | raw results: backend\scripts\results\chaos_test_20260824T113451Z.csv |
| 2026-08-24 | Backoff jitter comparison (generation pipeline) | scripts/compare_jitter.py | jitter=False, callers=20, base_backoff_seconds=1.0, max_attempts=3, fail_times=2 | round 1: stddev=0.000s, range=[1.000,1.000]s; round 2: stddev=0.000s, range=[2.000,2.000]s | in-process simulation, not a live HTTP run — see script docstring for why |
| 2026-08-24 | Backoff jitter comparison (generation pipeline) | scripts/compare_jitter.py | jitter=True, callers=20, base_backoff_seconds=1.0, max_attempts=3, fail_times=2 | round 1: stddev=0.230s, range=[0.034,0.906]s; round 2: stddev=0.608s, range=[0.062,1.952]s | in-process simulation, not a live HTTP run — see script docstring for why |
| 2026-08-24 | Circuit-breaker threshold comparison (generation pipeline) | scripts/compare_thresholds.py | fast-trip: failure_threshold=1, recovery_timeout_seconds=5.0, requests=15, fail_after=3, fail_for=6, mode=error | success_rate=0%, first_error_response=1 req, recovery=1 req | raw results: backend\scripts\results\chaos_test_20260824T115109Z.csv — part of a threshold comparison, see other row for the paired config |
| 2026-08-24 | Circuit-breaker threshold comparison (generation pipeline) | scripts/compare_thresholds.py | slow-trip: failure_threshold=5, recovery_timeout_seconds=30.0, requests=15, fail_after=3, fail_for=6, mode=error | success_rate=0%, first_error_response=1 req, recovery=None req | raw results: backend\scripts\results\chaos_test_20260824T115135Z.csv — part of a threshold comparison, see other row for the paired config |
